{# yay! #}
{% extends 'base.html.twig' %}
<h1>{{ title }}</h1>

<div>
    <p>Boeing is the best</p>
</div>

<h2>Comments ({{ comments|length }})</h2>

<ul>
    {% for comment in comments %}
        <li>{{ comment }}</li>
    {% endfor %}
</ul>